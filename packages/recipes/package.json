{
  "name": "@reactnetes/recipes",
  "version": "0.1.0",
  "description": "Pre-built Kubernetes component recipes for ReactNetes",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@reactnetes/core": "*",
    "@reactnetes/k8s-types": "*"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  }
}
