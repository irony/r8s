{
  "name": "@r8s/cli",
  "version": "0.1.0",
  "description": "CLI for rendering r8s TSX to Kubernetes YAML",
  "main": "./dist/index.js",
  "bin": {
    "r8s": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@r8s/core": "*",
    "@r8s/k8s-types": "*",
    "js-yaml": "^4.1.0",
    "esbuild": "^0.19.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vitest": "^1.0.0",
    "@types/js-yaml": "^4.0.9"
  }
}
