{
  "name": "@reactnetes/cli",
  "version": "0.1.0",
  "description": "CLI for rendering ReactNetes TSX to Kubernetes YAML",
  "main": "./dist/index.js",
  "bin": {
    "reactnetes": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@reactnetes/core": "*",
    "@reactnetes/k8s-types": "*",
    "js-yaml": "^4.1.0",
    "esbuild": "^0.19.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vitest": "^1.0.0",
    "@types/js-yaml": "^4.0.9"
  }
}
